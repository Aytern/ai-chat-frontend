import { defineConfig } from 'vite'
import vue from '@vitejs/plugin-vue'

// 最简单的配置
export default defineConfig({
  plugins: [vue()],
  server: {
    host: '0.0.0.0', // 允许外部访问
    port: 3000,      // 指定端口
    open: true       // 自动打开浏览器
  }
})

